{
  "name": "dig-uptime-watchdog",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker that restarts wedged Fly machines for dig-web and dig-api",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.30.0"
  }
}
