node_modules/
dist/
build/
# Local Next build output. The image builds its own inside the container, so
# shipping this only inflates the upload — a local `next build` leaves ~290MB
# here and turned a deploy into a 7-minute context transfer on 2026-08-08.
.next/
coverage/
.turbo/
.git/
.env
.env.*
!.env.example
*.log
data/
discogs_*.xml.gz
discogs_*.xml.gz.part
discogs_*_CHECKSUM.txt
.DS_Store
.vercel
docs/
